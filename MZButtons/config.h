#define KEYPADS_NO 5   //number of 4x4 key matrices attached. MAX is 8 (max 128 buttons total allowed, counting the rocker switches and encoders) MIN is 1
#define ENCODERS_NO 2   //number of rotary encoders attached. MAX is 2.
#define SWITCHES_NO 4   //number of toggle switches attached. MAX is 4.
#define USB_SIDE  1     //USB location: left side=0, right side=1  
